# 侧边栏 UI 设计优化方案

## 设计目标
优化面板、提示栏、扩展区域等侧边栏组件的视觉层级、可读性和交互体验。

## 核心优化点

### 1. 提升文字可读性
- 提示文本从 9-10px → 11px
- 标题从 13px → 14px
- 增加行高到 1.4-1.5

### 2. 优化间距系统
- 标题区域高度 48px → 42px
- 输入框间距增加到 14px
- 区域内边距统一为 12px

### 3. 增强交互反馈
- 输入框高度提升到 30px
- 聚焦时显示蓝色外发光
- 按钮悬停时轻微上移

### 4. 改进视觉层级
- 使用卡片式分组
- 增强区域间分隔线
- 优化图标可见度

### 5. 深色模式优化
- 提升对比度（12% → 14%）
- 优化边框颜色
- 增强输入框背景

## 实施方案
直接修改 `src/style.css` 文件中的相关样式。
